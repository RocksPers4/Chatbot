[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "gunicorn --bind 0.0.0.0:$PORT backend.app:app"

# Elimina healthcheckPath si no es necesario
healthcheckPath = "/"
healthcheckTimeout = 300

[variables]
PORT = "8080"  # Aquí defines el puerto que usará Railway
